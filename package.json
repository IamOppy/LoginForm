{
  "name": "css-display-property",
  "version": "1.0.0",
  "description": "block level elements span the whole page\r inline-level elements can have other elements that are inline or inline-block elements\r side by side.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongoose": "^6.7.2"
  }
}
